Synthetic self-signed localhost certificate and test-only private key, generated
for the Electron TLS rejection test. Never used for an account, deployment,
production service, or trust-store installation. The test requires rejection.
