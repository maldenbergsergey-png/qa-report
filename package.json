{
  "name": "qa-report-jira-editor",
  "private": true,
  "version": "0.4.0",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check jira-markup-import.js && node --check checklist-selection.js && node --check checklist-numbering.js && node --check release-notes.js && node --check pwa.js && node --check sw.js",
    "test": "node --test tests/*.test.js"
  }
}
